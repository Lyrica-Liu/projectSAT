Multiple-choice answer row with an A–D letter chip and graded color states.

```jsx
<AnswerOption letter="B" state="correct">Because the author qualifies the claim.</AnswerOption>
```

States: `default`, `selected` (lilac, pre-reveal), `correct` (mint), `incorrect` (rose), `muted` (the un-chosen others after reveal). Stack with `gap`.
