Square, rounded icon-only button for nav bars, toolbars and close actions.

```jsx
<IconButton label="Close" variant="surface"><i data-lucide="x"></i></IconButton>
```

Variants `ghost | surface | soft`, sizes `sm | md | lg`. Always pass `label` for accessibility. Pair with Lucide icons.
