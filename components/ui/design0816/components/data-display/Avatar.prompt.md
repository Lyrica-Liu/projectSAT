Circular initials chip; the pastel tone is hashed from the name so it's stable per user.

```jsx
<Avatar name="Maya Chen" size={40} />
```

Derives up to two initials. Pass `tone` (0–4) to force a specific pastel.
