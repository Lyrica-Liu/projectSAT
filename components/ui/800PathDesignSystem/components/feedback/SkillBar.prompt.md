Labeled accuracy row — skill name, percent, and a bar that auto-colors by score (≥75 mint, ≥50 butter, else rose). The dashboard/results skill breakdown.

```jsx
<SkillBar label="Inferences" accuracy={68} detail="7/10 · 68%" />
```

Stack several with `gap` to form the breakdown list.
