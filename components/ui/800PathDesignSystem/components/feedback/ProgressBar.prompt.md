Slim rounded progress track — session completion, generic percentages.

```jsx
<ProgressBar value={60} tone="brand" showLabel />
```

`tone` colors the fill (`brand | success | warning | danger`). Use for the question-session progress bar.
