Large selectable option row with a radio indicator — practice setup (domain, length).

```jsx
<ChoiceCard label="Reading & Writing" desc="Mix of both domains" selected={d==='both'} onClick={()=>setD('both')} />
```

Selected state tints lilac with a focus glow. Stack with `gap`.
