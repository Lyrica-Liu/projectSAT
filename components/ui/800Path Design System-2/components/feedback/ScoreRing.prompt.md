Circular score dial with a big centered percentage — the results-page hero metric.

```jsx
<ScoreRing score={82} caption="8 of 10 correct" />
```

Auto-colors by score (≥80 mint, ≥60 butter, else rose). Size/stroke adjustable.
